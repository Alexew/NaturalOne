@inherits FluxorComponent

@inject IState<BuilderState> RaceState

@if (RaceState.Value.Race is not null)
{
    foreach (var trait in RaceState.Value.Race.Traits)
    {
        <MudChip>@trait</MudChip>
    }
}